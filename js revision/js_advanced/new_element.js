//! to replace element using dom 

{/* <p id="old-paragraph">This is the old paragraph.</p>

// 1. Create the new element
const newHeading = document.createElement('h1');
newHeading.textContent = 'This is the new heading!';

// 2. Find the old element you want to replace
const oldParagraph = document.getElementById('old-paragraph');

// 3. Call replaceWith() on the old element */}
// oldParagraph.replaceWith(newHeading);


//!to delete  element of dom 
 
{/* <p id="goodbye">This paragraph will be removed.</p>
    
    // 1. Find the element you want to remove
const elementToRemove = document.getElementById('goodbye');

// 2. Call the .remove() method on it
elementToRemove.remove();*/}